package com.zooz.common.client.ecomm.beans.enums;

/**
 * The RefundReason enum is used to select the reason for the refund.
 */
public enum RefundReason {
    GENERAL_REFUND,
    SERVICE_NOT_RENDER,
    GOODS_NOT_PROVIDED,
    GOODS_RETURNED
}
